# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class
*.pyc
*.py~

# Distribution / packaging
.Python
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
tags
MANIFEST

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Dependency directories
node_modules/
jspm_packages/

# IDEs and editors
.vscode/
.vs/
.idea/
.kdev4/
*.kdev4
*.DS_Store
*.swp
*.comp.js
.wnf-lang-status
*debug.log

# Helix Editor
.helix/

# Aider AI Chat
.aider*
.mcp.json
everything-claude-code/

# Frontend build output
benchpress/public/frontend/
benchpress/www/frontend.html
# bench build symlinks this into the app; it is not source
benchpress/public/node_modules

# Design handoffs and other binary drops land here while a phase is in flight;
# they are not part of the published repository.
*.zip
handoff*/
*.pen

# Local tooling artifacts (knowledge graph + scratch specs)
graphify-out/
specs/
.serena/

# Playwright run artifacts
test-results/

# Local-only working notes, drafts and generated reports. None of these are part
# of the published repository, and the Ralph loops refuse to branch over an
# untracked file, so anything that lives only on this box belongs here.
todo.md
brainstrom.txt
AI_AGENT_HARNESS_ENGINEERING_PLAYBOOK.md
*.docx
architecture-review-*.html
docs/design/

# Skills that ship from the Shipcraft plugin marketplace, not from this repo.
.claude/skills/improve-codebase-architecture/
.claude/skills/wayfinder/

# Three leadtype artifacts stamp a build clock into their body, so two runs of
# `npm run docs:build` can never be byte-identical. Everything else it generates
# — the markdown mirrors, AGENTS.md, llms.txt, sitemap.xml, robots.txt — is
# committed and gated against hand edits by the CI docs job.
docs-site/docs/agent-readability.json
docs-site/docs/search-content.json
docs-site/docs/search-index.json
.ralph/

.teach/

# Marketing and commercial strategy. The software is open source; the go-to-market is not.
.marketing/
.agents/product-marketing.md

# Agent working directories.
.claude/worktrees/
