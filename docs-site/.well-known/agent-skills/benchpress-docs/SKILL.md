---
name: benchpress-docs
description: Read and search the BenchPress documentation. Use when working with BenchPress — its setup, configuration, API, and behavior.
metadata:
  source: leadtype
---
# BenchPress documentation

Press a button. Get a Frappe bench.

To work with BenchPress, read its documentation:

- Start at `/llms.txt` (task-routed index) or `/llms-full.txt` (full context).
- Every page is also available as Markdown at its `.md` URL.
